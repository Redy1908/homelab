---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homepage-pvc
  namespace: homepage
spec:
  storageClassName: manual
  volumeName: homepage-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
